# Klima docs

Scaffold documentation site for the Klima monorepo.

This is **not** a full product docs portal yet. See the repo root
[`STATUS.md`](https://github.com/Signal-No-5/klima/blob/HEAD/STATUS.md).

## Packages

| Path | Role |
|------|------|
| `mobile/` | Flutter app |
| `backend/` | FastAPI + pipeline |
| `frontend/` | LGU dashboard stub |
| `data/` | ETL pointer (pipeline lives in `backend/pipeline`) |
