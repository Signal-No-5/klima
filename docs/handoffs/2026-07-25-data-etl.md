# HANDOFF — Data owns ETL (#8)

**Status:** Planned  
**Branch:** `feat/mvp-data`  
**Issue(s):** #8  
**Last updated:** 2026-07-25

## Bottom line
`data/` becomes canonical home for PAGASA bronze pipeline + DuckDB outputs; one documented run command; backend reads from there.

## Current state
- Pipeline under `web/api/pipeline`
- `data/` is stub pointing at API pipeline

## Hook points
- Move/rehome `pipeline/` → `data/pipeline/`
- `scripts/run_pipeline.py`
- Backend config paths for DuckDB files

## How to verify
```bash
cd data && uv run python -m pipeline...   # exact entrypoint after move
ls data/**/*.duckdb
```

## Done means
- [ ] Not pointer-only
- [ ] Documented command succeeds
- [ ] Backend/docs reference data as SoT
