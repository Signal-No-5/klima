# HANDOFF — Docs site (#7)

**Status:** Done (verified build)  
**Branch:** `feat/mvp-docs-7`  
**Issue(s):** #7  
**Last updated:** 2026-07-25

## Bottom line
VitePress under `docs/` builds with monorepo layout, per-component quickstarts (backend / frontend / mobile / data / docs), API overview from real FastAPI routes, and an honest schema page (`backend/app/schemas/klima.py` today; central `schema/` still #4).

## Current state
- VitePress product docs: `index`, `layout`, `getting-started`, `guide/*`, `api/overview`, `schema`
- `docs/mvp/` + `docs/handoffs/` remain in-repo; excluded from site build via `srcExclude` (cleaner than shipping agent templates through Vue markdown)
- Config is `.vitepress/config.mts` + `"type": "module"` (Node 22 ESM)

## Hook points
- VitePress config, guide pages, API overview
- OpenAPI linked as running-server URLs (`/docs`, `/openapi.json`) — not an embedded copy

## How to verify
```bash
cd docs && pnpm i && pnpm docs:build
```

## Done means
- [x] Build succeeds (`pnpm docs:build`)
- [x] Pages listed in epic DoD present (index, layout, backend/frontend/data QS, schema, API overview)
- [x] STATUS honesty preserved
- [x] Internal site links resolve (no dead relative hrefs in dist)
