# Klima API base URL (no trailing slash). Used by server-side fetches in the dashboard.
# Points at Fly in production or local compose in dev; set real values in the
# Vercel project dashboard rather than committing them.
NEXT_PUBLIC_KLIMA_API_URL=http://127.0.0.1:8000
